import Variables from "../features/Variables";

let variables = {
    myBrandNewVar: 1,
    speaker: {
        color: "#010031"
    }
};

variables = new Variables(variables);

export default variables;